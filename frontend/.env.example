# Optional. Leave these unset and the frontend derives both hosts from the
# address the page was opened on, so a phone on the LAN works with no edits.
# Set them only when the API or AI service lives somewhere else.

# VITE_API_URL=http://localhost:8080
# VITE_AI_URL=http://localhost:8000
